#!/bin/sh
# Week 2: Pre-push validation
# Runs before EVERY push - comprehensive checks
# Target: < 5 minutes

echo "🚀 Pre-push: Running comprehensive validation..."

# 1. Full unit test suite with coverage (~30 sec)
echo "  → Running full test suite with coverage..."
npm run test:coverage || {
  echo "❌ Tests failed or coverage below threshold. Please fix and try again."
  exit 1
}

# 2. Build verification (~1-2 min)
echo "  → Building for production..."
npm run build || {
  echo "❌ Build failed. Please fix errors and try again."
  exit 1
}

# 3. E2E critical paths (skip if browsers not installed) (~1.5 min)
if [ -d "$HOME/Library/Caches/ms-playwright" ]; then
  echo "  → Running E2E critical path tests..."
  npm run test:e2e:critical || {
    echo "❌ E2E tests failed. Please fix and try again."
    exit 1
  }
else
  echo "  ⚠️  Skipping E2E tests (Playwright browsers not installed)"
  echo "     Run 'npx playwright install' to enable E2E tests in pre-push hook"
fi

echo "✅ Pre-push checks passed! Safe to push."
